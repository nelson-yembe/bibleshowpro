{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for main and output windows",
  "windows": ["main", "output"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default",
    "dialog:default"
  ]
}
